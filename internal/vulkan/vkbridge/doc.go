// Package vkbridge isolates the small handwritten Vulkan cgo surface this
// project needs, so the renderer stays focused on rendering code instead of
// cgocheck-safe marshalling details.
package vkbridge
